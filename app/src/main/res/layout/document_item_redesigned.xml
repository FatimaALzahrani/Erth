<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/document_item_redesigned.xml -->
<!-- This layout is used inside the Documents section -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="12dp"
    android:paddingHorizontal="8dp"
    android:background="?attr/selectableItemBackground">

    <ImageView
        android:id="@+id/iv_doc_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="16dp"
        android:src="@drawable/ic_document" 
        app:tint="@color/primary_dark"
        />

    <TextView
        android:id="@+id/tv_doc_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@+id/iv_doc_icon"
        android:layout_toStartOf="@+id/iv_doc_download"
        android:layout_centerVertical="true"
        android:layout_marginEnd="16dp"
        android:textColor="#3F3D56"
        android:textSize="14sp"
        tools:text="اسم المستند" />

    <ImageView
        android:id="@+id/iv_doc_download"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:src="@drawable/ic_document"
        app:tint="#8A8A8E" 
       />

</RelativeLayout>
