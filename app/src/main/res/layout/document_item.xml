<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="16dp"
    android:paddingHorizontal="16dp"
    android:background="?attr/selectableItemBackground">

    <ImageView
        android:id="@+id/iv_doc_icon" 
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:src="@drawable/ic_document" 
        app:tint="@color/primary_dark" />

    <TextView
        android:id="@+id/tv_doc_title" 
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toStartOf="@id/iv_doc_icon"
        android:layout_centerVertical="true"
        android:layout_marginEnd="16dp"
        android:textColor="#3F3D56"
        android:textSize="14sp"
        tools:text="عنوان المستند" />

    <!-- Optional: Add an icon at the start for download/link indication -->
    <!-- <ImageView
        android:id="@+id/iv_doc_link_indicator"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:src="@drawable/ic_link" 
        app:tint="#8A8A8E"
        android:visibility="gone" /> -->

</RelativeLayout>
